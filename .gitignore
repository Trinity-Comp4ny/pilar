# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
supabase/.temp
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.*
!.env.example
coverage
.eslintcache
*.tsbuildinfo

# Supabase generated
supabase/schema.sql
supabase/.branches/
# Edge function secrets (nunca commitar)
supabase/functions/.env
supabase/.env
backups/

# Claude Code worktrees (temporary agent workspaces)
.claude/worktrees/

# temporários do gate de sincronia de tipos (job database do CI)
/types-gate-*.ts

# Artifacts do Playwright (achado real, 17/08: faltavam aqui — um `npx playwright
# test` local esteve a um `git add -A` de commitar screenshot/vídeo/trace de
# sessão autenticada real, incluindo o storageState em .auth/).
/playwright-report/
/test-results/
/.auth/

# Saída local da skill /graphify (grafo de conhecimento do código)
# sem barra inicial: cobre o graphify-out/ da raiz E o cache de AST que ele
# cria dentro de src/graphify-out/ quando roda escopado numa subpasta
graphify-out/

# Preview local dos templates de e-mail (npm run email:preview)
.email-preview/

# Deno 2.x local gera lockfile v5; o CI e o runtime do Supabase rodam Deno 1.45
deno.lock
